// Copy this file to config.js and fill in your Mapbox access token.
// config.js is gitignored so the token is never committed to the repository.
//
// Get a free token at https://account.mapbox.com/access-tokens/ and restrict
// it to your site's domain(s) in the Mapbox dashboard, since it is served
// publicly on the static page.
window.RUNSKITIROL_CONFIG = {
  mapboxToken: "",
  mapboxStyle: "mapbox/outdoors-v12",
};
